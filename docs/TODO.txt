Block module có thể đặt tệp php ở giao diện
Có thể dùng hàm nv_format_date trong js
Dùng get_block_tpl_dir để xác định giao điện tương tự get_module_tpl_dir
Dùng hàm nv_outdated_browser() để phát hiện trình duyệt lỗi thời
Dùng hàm addition_module_assets để load js, css module khác vào
Dùng nv_captcha_form_attrs để lấy thuộc tính captcha form khỏi viết code if cực khổ
